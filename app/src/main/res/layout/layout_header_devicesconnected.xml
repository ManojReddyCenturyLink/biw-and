<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="10dp"
    android:paddingTop="10dp">

    <ImageView
        android:id="@+id/faq_header_arrow"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_margin="16dp"
        android:layout_marginEnd="16dp"
        android:scaleType="fitXY"
        android:tint="@color/blue"
        app:layout_constraintBottom_toBottomOf="@id/faq_header_group_title"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/faq_header_group_title"
        app:srcCompat="@drawable/ic_icon_right"
        android:contentDescription="@string/content_description_holder"
        />

    <TextView
        android:id="@+id/faq_header_group_title"
        android:layout_width="0dp"
        android:fontFamily="@font/arial_mt"
        android:layout_height="wrap_content"
        android:paddingStart="12dp"
        android:textColor="@color/black"
        android:textSize="@dimen/small_text_size"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/faq_header_arrow"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="RtlSymmetry"
        />

    <View
        android:id="@+id/faq_dividers_view"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginTop="@dimen/margin_spacing_small"
        android:background="@color/light_grey"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/faq_header_arrow" />
</androidx.constraintlayout.widget.ConstraintLayout>